# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

ripper:
  name: Ripper
  # pairing_token: nil
  access_token: <%= SecureRandom.hex(18) %>
  paired: true
  status: 1

thread:
  name: Thread
  pairing_token: <%= SecureRandom.hex(18) %>
  # access_token: nil
  paired: false
  status: 1
